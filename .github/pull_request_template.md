## What does this PR do?

(Brief description of the change and why it's needed)

## Type of change

- [ ] Bug fix
- [ ] New feature
- [ ] Documentation
- [ ] Refactor / code quality

## Checklist

- [ ] `npm test` passes
- [ ] `npm run lint` passes
- [ ] `npx tsc --noEmit` passes
- [ ] Mock mode still works with no `.env` file (`npm run dev` from scratch)
- [ ] No secrets, personal data, or `.env` values committed
- [ ] CLAUDE.md conventions followed (commit message format, no `console.log`, no `any` casts)

## Screenshots (if UI change)

(Before / after if relevant)
